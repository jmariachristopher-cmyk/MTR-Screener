# Copy this file to .streamlit/secrets.toml for LOCAL use only.
# Never commit the real secrets.toml to GitHub -- add it to .gitignore.
# On Streamlit Community Cloud, set this instead via
# App settings -> Secrets (do not put it in the repo at all).

UPSTOX_ACCESS_TOKEN = "paste-todays-access-token-here"
